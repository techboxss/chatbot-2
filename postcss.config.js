export default {
  plugins: {
    // Keep this empty. 
    // @tailwindcss/vite handles the CSS processing.
    // This file only exists to stop Vite from finding the global config in your home directory.
  }
}